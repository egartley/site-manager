<Page x:ConnectionId='1'
    x:Class="Site_Manager.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:site_manager="using:Site_Manager"
    mc:Ignorable="d"
                        >

    <Page.Resources>
        <site_manager:StringFormatConverter x:Key="StringFormatConverter" />
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" MaxWidth="900">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Style="{StaticResource ContentTitleTextBlockStyle}">Managed Web Pages</TextBlock>
            <ListView x:ConnectionId='3' Grid.Row="1" Name="WebPagesListView"                                        MinHeight="300" MaxHeight="750" HorizontalAlignment="Stretch" Style="{StaticResource ListViewBorderStyle}" IsItemClickEnabled="True" Margin="0,16,0,0">
                <ListView.ItemTemplate>
                    <DataTemplate                                          >
                        <StackPanel x:ConnectionId='7' Margin="0,14,0,14">
                            <TextBlock x:ConnectionId='8'                     FontWeight="SemiBold" FontSize="19" />
                            <TextBlock x:ConnectionId='9'                           FontSize="12" Margin="0,3,0,0" Foreground="{ThemeResource AccentButtonBackground}" />
                            <TextBlock x:ConnectionId='10'                                                                                                                       FontSize="12" Margin="0,3,0,0" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Grid Margin="0,12,0,0" Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <AutoSuggestBox x:ConnectionId='5' Name="NewPageTextBox" PlaceholderText="https://egartley.net"                                                                                                                                                                          />
                <Button x:ConnectionId='6' Grid.Column="1" Name="AddNewPageButton" Margin="8,0,0,0" Height="32" Width="72"                                Style="{StaticResource ButtonRevealStyle}">Add</Button>
            </Grid>
            <TextBlock x:ConnectionId='4' Grid.Row="3" Name="AddNewPageStatusTextBlock" Foreground="Red" FontWeight="Bold" Margin="4,12,0,0">
                <TextBlock.Transitions>
                    <TransitionCollection>
                        <ContentThemeTransition />
                    </TransitionCollection>
                </TextBlock.Transitions>
            </TextBlock>
        </Grid>
        <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button x:ConnectionId='2' Name="DeployButton"                            Visibility="Visible" FontSize="16" Style="{StaticResource ContentOnlyButtonStyle}">
                <Grid>
                    <Border CornerRadius="85" Width="170" Height="170" Background="{ThemeResource AccentButtonBackground}">
                        <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" TextAlignment="Center" FontSize="26" LineHeight="35">
                            <Run>Deploy</Run><LineBreak/><Run>Everything</Run>
                        </TextBlock>
                    </Border>
                </Grid>
            </Button>
        </StackPanel>
    </Grid>
</Page>

